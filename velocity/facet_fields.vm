#if($response.facetFields)
    <h2 #annTitle("Facets generated by adding &facet.field= to the request")></h2>
    #foreach($field in $response.facetFields)
      <ul>
        #if ($request.params.get("fq") && $request.params.get("fq").length()>0)
          #set ($checked="checked")
        #else
          #set ($checked="")
        #end
        #foreach($facet in $field.values)
            <li>$facet.name ($facet.count)</li>
        #end
      </ul>
    #end
  #end
